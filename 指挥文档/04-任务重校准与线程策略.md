# DD Hermes 当前任务重校准与线程策略

## 当前单一判断

- 一期已经 `phase done`。
- 最近归档的 phase-2 proof task 是 `dd-hermes-independent-quality-seat-v1`。
- 当前 phase-2 唯一主线是 `dd-hermes-quality-seat-escalation-rules-v1`。
- 当前线程继续作为唯一默认指挥线程。
- 当前主线已经完成第一条 execution slice，并且有 execution commit、closeout、expert 回传 handoff 和质量审查证据。

## 当前主线到底在做什么

`dd-hermes-quality-seat-escalation-rules-v1` 的目标只有一条：

- 把 DD Hermes 的初始任务分类矩阵压成共享控制面的真实规则，让维护者能明确知道哪些任务类允许 `degraded`，哪些任务类必须 `requires-independent`。

当前这条主线已经完成的第一条 execution slice 包括：

- 冻结初始矩阵：`T0/T1/T2 => degraded-allowed`，`T3/T4 => requires-independent`
- 把 `task_class / quality_requirement / task_class_rationale` 落进 `state / context / dispatch / gates / schema / sprint bootstrap`
- 回填历史 v2 proof task，使旧任务在新规则下仍能通过 schema revalidation

不在本主线里的内容：

- 新的 Hermes runtime、gateway、provider、plugin loader
- 为了“像多 agent”而额外制造新的聊天线程
- 把治理任务和无关功能扩展混成一个大 task

## 当前主线状态

- `planning`：已完成
- `decision synthesis`：已完成
- `first execution slice`：已完成并收口证据
- `task archive`：未完成

当前真实停点不是“还没开始写”，而是：

- 第一条 execution slice 已经收口
- 当前 task 仍然是 active mainline
- 下一步要决定是直接 archive，还是继续一条更窄的 override slice

## 当前唯一合理的下一步

当前只允许两种后续动作：

1. 归档 `dd-hermes-quality-seat-escalation-rules-v1`
   - 前提：认为“初始矩阵 + 历史兼容迁移 + gate 证明”已经满足任务边界
2. 继续同一主线，但只允许做更窄的下一条 slice：
   - 显式定义哪些 `T2` bounded slice 必须手动升级为 `requires-independent`

当前不应该做的事情：

- 重新打开 `dd-hermes-independent-quality-seat-v1`
- 回到抽象角色理论或泛化文档整理
- 把主线扩回线程模型、runtime 或 provider 层

## 继续编码前最小读取集合

1. `workspace/contracts/dd-hermes-quality-seat-escalation-rules-v1.md`
2. `workspace/state/dd-hermes-quality-seat-escalation-rules-v1/state.json`
3. `workspace/decisions/quality-seat-escalation-routing/synthesis.md`
4. `workspace/handoffs/dd-hermes-quality-seat-escalation-rules-v1-lead-to-expert-a.md`
5. `workspace/handoffs/dd-hermes-quality-seat-escalation-rules-v1-expert-a-to-lead.md`
6. `workspace/closeouts/dd-hermes-quality-seat-escalation-rules-v1-expert-a.md`

## 现在需不需要第二线程

不是默认需要。

当前更合理的形态仍然是：

- `一个主线程 + 线程内多角色切换 + 按需隔离 worktree`

只有同时满足下面条件，才值得外派新的 execution slice：

- 已有明确 `task_id`
- 已有 `contract + state + context + handoff + worktree`
- 已有 `synthesis_path`
- 写集边界稳定
- 额外 worktree 能真实降低冲突或回归风险

如果当前工作还是裁决、归档、证据回填或文档收口，就不该开第二线程。

## 多 Agent 怎么用

- 架构、控制面、线程模型、权限/安全边界，默认先走 `3-explorer-then-execute`
- 这里的“多 agent”首先是当前线程内的协议角色与内部 explorer 收敛，不要求用户手动管理多个聊天线程
- 用户只保留当前线程作为对话面；其他线程如果存在，只作为系统内部执行面或历史来源，由系统自行生成、吸收、关闭

## 管理职责落在哪里

不存在一个必须长期在线的“管理 agent 的 agent”。

当前真实设计是：

- `Supervisor / Product Anchor` 负责目标压缩、主线裁决和状态推进
- `Skeptic / Quality Anchor` 负责独立审查或显式降级
- 管理真相落在 `state + context + gate`，不是落在一个永久聊天线程上
