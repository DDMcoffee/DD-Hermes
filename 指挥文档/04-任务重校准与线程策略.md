# DD Hermes 当前任务重校准与线程策略

## 当前单一判断

- 一期已经 `phase done`。
- 最近归档的 phase-2 proof task 已更新为 `dd-hermes-successor-evidence-audit-v1`。
- 最近归档的治理主线仍是 `dd-hermes-successor-triage-v1`。
- 当前没有 active mainline。
- 当前线程继续作为唯一默认指挥线程。
- repo 当前不再卡在“successor evidence 只能靠手工 sweep 判断”，因为这条 proof 已经把 audit endpoint、entry summary、execution closeout、quality review 和 integration truth 都收口了。
- 当前真实停点已经回到“没有 active mainline；下一步只能重新做 successor triage”。

## 最近这条 proof 刚刚证明了什么

`dd-hermes-successor-evidence-audit-v1` 刚刚完成并归档，核心结论是：

1. `successor.audit` 已经把 committed live candidates、archived proof history 和 working-tree residue 的区分做成共享 endpoint，而不是继续停留在手工 shell sweep。
2. `demo-entry` 已能消费这份 successor truth，所以“现在有没有主线、为什么没有”不再依赖聊天记忆或静态 prose 猜测。
3. 这条 proof 已经有 review-backed execution closeout，execution anchor 是 `897a0d58f462ff7c4525e414682f037e023ac839`，并已整合进 shared root，integration commit 是 `67d50f80fb8e4f8fa937e6eeaf772e4763c1b231`。
4. archive 后 repo 又回到了诚实的“最近 proof 已归档、当前没有 active mainline”状态。

## 最近归档 proof 到底做了什么

`dd-hermes-successor-evidence-audit-v1` 的目标只有一条：

- 让 DD Hermes 在需要判断“有没有下一条主线 / 哪些只是 residue”时，能从共享 control-plane truth 里给出一个可调用、可审查、可复验的答案。

当前已完成的 bounded slice：

- `scripts/coordination-endpoint.sh --endpoint successor.audit` 现在会返回 committed candidate / archived proof / residue 三类真相
- `scripts/demo-entry.sh` 已接入 successor-audit truth，在 shared root 下能直接解释当前 mainline 状态
- `tests/smoke.sh` 已覆盖 entry / endpoint / residue-aware successor verdict 行为
- 当前主线自己的 `expert-a` execution slice 已完成真实 execution commit、independent skeptic review、integration 和 archive 证据

不在这条已归档 proof 里的内容：

- 新的 runtime、gateway、provider 或 plugin-loader
- 再造一个新的多线程产品面
- 在没有新 task package 的情况下替 repo 擅自选择 successor

## 当前 phase-2 状态

- `planning`：已完成
- `candidate-pool cleanup`：已完成
- `successor selection`：上一轮已完成；accepted path 是 `dd-hermes-independent-skeptic-dispatch-v1`
- `skeptic lane materialization`：已完成
- `dispatch failure reporting hardening`：已完成
- `execution slice + quality review + closeout`：已完成
- `integration + archive`：已完成
- `successor evidence audit`：已完成并 archive（`dd-hermes-successor-evidence-audit-v1`）

当前真实停点已经收口成：

- 最近 proof 已归档：`dd-hermes-successor-evidence-audit-v1`
- 最近治理裁决已归档：`dd-hermes-successor-triage-v1`
- 当前 active mainline：暂无
- repo 不再卡在 successor evidence audit proof；当前真实停点是重新做 successor triage，并且只有 repo evidence 明确支持时才立下一条主线

## 现在应该做什么

当前唯一合理的下一步，不是重开已归档 proof，也不是从 residue 或聊天记忆里硬凿新主线，而是：

1. 重新做 successor triage
2. 只有在 repo evidence 明确支持一个新 bounded task package 时，才重新写入 `current_mainline_task_id`
3. 如果证据仍然不足，就维持“当前没有 active mainline”的诚实状态

当前不应该做的事情：

- 重新打开 `dd-hermes-successor-evidence-audit-v1`、`dd-hermes-independent-skeptic-dispatch-v1` 或其他已归档 proof
- 为了看起来“更大”而扩成 runtime、provider 或 scheduler 任务
- 因为最近 proof 做完了，就从 residue 或聊天记忆里硬立下一条 feature 主线

## 当前主线裁决

当前结论已经更新为：**`dd-hermes-successor-evidence-audit-v1` 已经 archive；当前没有 active mainline。**

理由：

1. 最近一轮 proof 已经把 successor evidence discrimination 收口成 shared endpoint / entry truth；这条 bounded gap 已经完成。
2. `review-policy-demo` 之类 residue 仍需明确标成 non-evidence，不能继续靠人工记忆处理。
3. 在没有新的 bounded task package 前，repo 只能诚实回到“no active mainline”。

## 继续编码前最小读取集合

1. `openspec/archive/dd-hermes-successor-evidence-audit-v1.md`
2. `workspace/decisions/successor-evidence-audit-routing/synthesis.md`
3. `workspace/closeouts/dd-hermes-successor-evidence-audit-v1-expert-a.md`
4. `workspace/contracts/dd-hermes-successor-evidence-audit-v1.md`
5. `指挥文档/06-一期PhaseDone审计.md`

## 现在需不需要第二线程

不是默认需要。

当前更合理的形态仍然是：

- `一个主线程 + 线程内多角色切换 + 按需隔离 worktree`

最近这条 proof 已经说明，真正需要的不是用户管理第二聊天线程，而是 DD Hermes 内部把 skeptic 角色物化成真实 review lane，并继续由主线程消费结果。

## 多 Agent 怎么用

- 架构、控制面、线程模型、权限/安全边界，默认先走 `3-explorer-then-execute`
- 这里的“多 agent”首先是当前线程内的协议角色与内部 explorer 收敛，不要求用户手动管理多个聊天线程
- 用户只保留当前线程作为对话面；其他线程如果存在，只作为系统内部执行面或历史来源，由系统自行生成、吸收、关闭

## 管理职责落在哪里

不存在一个必须长期在线的“管理 agent 的 agent”。

当前真实设计是：

- `Supervisor / Product Anchor` 负责目标压缩、主线裁决和状态推进
- `Skeptic / Quality Anchor` 负责独立审查或显式降级
- 管理真相落在 `state + context + gate`，不是落在一个永久聊天线程上
