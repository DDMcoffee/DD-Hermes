# DD Hermes 当前任务重校准与线程策略

## 当前单一判断

- 一期已经 `phase done`。
- 最近归档的 phase-2 proof task 已更新为 `dd-hermes-legacy-archive-normalization-v1`。
- 最近归档的治理主线已更新为 `dd-hermes-successor-triage-v1`。
- 当前 active mainline 已切到 `dd-hermes-independent-skeptic-dispatch-v1`。
- 当前线程继续作为唯一默认指挥线程。
- 最近完成的 proof 不是新 feature，而是 archive-control-plane normalization：修复旧 proof 在今天这套 contract/state/verdict/closeout 体系下的失真。
- 当前真实停点不再是“下一条主线选谁”，而是“如何把真实独立 skeptic 从命名真相推进成运行面真相”。
- 共享控制面上的第一刀已经落下：独立 skeptic lane 现在会物化成 worktree + handoff + role-specific context/runtime packet，而不是只留在 state 名字里。
- 当前剩余卡点也更窄了：这条 active mainline 已经有了自己的 `expert-a` / `expert-b` lanes，execution gate 也已通过；接下来缺的只是真实 execution commit、独立质量复核和 closeout/archive 证据。

## 最近这条 proof 刚刚证明了什么

`dd-hermes-legacy-archive-normalization-v1` 刚刚把 archive truth 拉平，核心结论是：

1. archived `T0/T1` task 现在会诚实暴露 `execution_closeout = not-required`，不再假性 blocked。
2. 一批 legacy archived proof contract / closeout / state 已经补回 schema-v2 的 `product / quality / verdict` 真相。
3. 这条 normalization proof 自己也已经有了 execution anchor `c0237e5163a9cb8d5477765b586f8571bb7370d6`，并已 archive。

所以现在 repo 的问题不再是“旧 proof 失真”，而是“真实独立 skeptic 到底什么时候才算被 DD Hermes 真正 materialize 出来”。

`dd-hermes-successor-triage-v1` 已经把“下一条 successor 到底该不该立、立哪一条”裁决完了，accepted path 是：

1. 不重开 `dd-hermes-explicit-gate-verdicts-v1`、`dd-hermes-quality-seat-escalation-rules-v1` 或更早的 archive proof
2. 不再维持“当前没有 active mainline”的空档状态
3. 把下一条主线收口成 `dd-hermes-independent-skeptic-dispatch-v1`

## 最近归档 proof 到底做了什么

`dd-hermes-legacy-archive-normalization-v1` 的目标只有一条：

- 让 DD Hermes 的 archived proof 在今天的共享控制面上继续说真话，不再因为旧 schema 或 placeholder closeout 被误判成未完成。

当前已完成的 bounded slice：

- `scripts/artifact_semantics.py` 现在把 `task_class_bucket = no-execution` 解释成 `execution_closeout = not-required`
- `scripts/check-artifact-schemas.sh` 不再拿 `T0/T1` 的 placeholder closeout 继续制造 semantic false negative
- `tests/smoke.sh`、`docs/artifact-schemas.md`、`docs/coordination-endpoints.md` 已同步到同一条 no-execution 语义
- 一批 legacy archived proof contract 已补齐 schema-v2 的 `product_goal / user_value / task_class / quality_requirement / non_goals / product_acceptance / drift_risk`
- 代表性 legacy execution closeout 已补齐 `quality_review_status / quality_findings_summary / Quality Review`
- 代表性 legacy archived state 已刷新到 v2，并重新对齐 execution anchor、degraded ack 和 quality review truth

不在本主线里的内容：

- 新的 runtime、gateway、provider 或 plugin-loader
- 再造一个新的多线程产品面
- 把 archive normalization 扩成 generic repo cleanup

## 当前 phase-2 状态

- `planning`：已完成
- `candidate-pool cleanup`：已完成
- `successor selection`：上一轮已完成；accepted path 是 `dd-hermes-legacy-archive-normalization-v1`
- `no-execution closeout semantics slice`：已完成
- `legacy contract/state/closeout backfill slice`：已完成并通过代表性 schema 校验
- `workflow/context/smoke verification`：已完成
- `execution anchor + archive`：已完成

当前真实停点已经收口成：

- 最近 proof 已归档：`dd-hermes-legacy-archive-normalization-v1`
- 最近治理裁决已归档：`dd-hermes-successor-triage-v1`
- 当前 active mainline：`dd-hermes-independent-skeptic-dispatch-v1`
- repo 不再卡在“选谁做 successor”，也不再卡在“缺 baseline”；现在卡点是当前主线自己的 execution/quality-review/closeout 证据还没写出来

## 当前这条主线正在做什么

当前唯一合理的 phase-2 工作，不是重开已归档 proof，也不是再写一层政策，而是把真实独立 skeptic 做成一条运行面主线：

1. 把“独立 skeptic 已命名”推进到“独立 skeptic lane 已 materialize”
2. 复用现有 dispatch/context/handoff/worktree/gate 面，而不是再造 runtime 或外显多线程产品面
3. 保留 degraded fallback 的诚实路径，不把它伪装成独立监督

已经落下的当前 slice：

- `dispatch-create` 现在会为独立 skeptic 物化真实 review lane，而不是只给 repo-root placeholder 命令
- `context-build` / `state-read` / `thread-switch-gate` 现在都能区分 `skeptic_lane ready|blocked|not-required`
- 共享 schema / smoke / docs 已同步到这条新 truth
- 当前主线自己的 `expert-a` executor lane 与 `expert-b` skeptic lane 已经从基线提交 `a0f30fd` 物化出来，`thread-switch-gate` 对 execution 已 pass

当前不应该做的事情：

- 重新打开 `dd-hermes-explicit-gate-verdicts-v1` 或 `dd-hermes-quality-seat-escalation-rules-v1`
- 为了看起来“更大”而扩成 runtime、provider 或 scheduler 任务
- 把“有单独 skeptic 名字”直接等同于“有真实独立 review lane”
- 把“lane 已物化”直接等同于“当前 task 已完成”；现在还缺 task-local execution / review / closeout 证据

## 当前主线裁决

当前结论已经更新为：**把 `dd-hermes-independent-skeptic-dispatch-v1` 设为当前 active mainline。**

理由：

1. 语义层、政策层、verdict 层都已经各自收口成 archive proof，继续回头做这些会反过来制造重复叙事。
2. 多份已归档 handoff / closeout 都把“真实独立 skeptic assignee”留成下一条 proof point。
3. 当前代码面也支持这个判断：`dispatch-create` 只给 executor 物化隔离执行面，skeptic 还没有等价的独立 review lane。

## 继续编码前最小读取集合

1. `workspace/contracts/dd-hermes-independent-skeptic-dispatch-v1.md`
2. `workspace/decisions/independent-skeptic-dispatch-routing/synthesis.md`
3. `workspace/handoffs/dd-hermes-execution-bootstrap-expert-a-to-lead.md`
4. `workspace/closeouts/dd-hermes-independent-quality-seat-v1-expert-a.md`
5. `指挥文档/06-一期PhaseDone审计.md`

## 现在需不需要第二线程

不是默认需要。

当前更合理的形态仍然是：

- `一个主线程 + 线程内多角色切换 + 按需隔离 worktree`

当前主线虽然涉及独立 skeptic，但真正需要的不是用户管理第二聊天线程，而是 DD Hermes 内部把 skeptic 角色 materialize 成真实 review lane，并继续由主线程消费结果。

## 多 Agent 怎么用

- 架构、控制面、线程模型、权限/安全边界，默认先走 `3-explorer-then-execute`
- 这里的“多 agent”首先是当前线程内的协议角色与内部 explorer 收敛，不要求用户手动管理多个聊天线程
- 用户只保留当前线程作为对话面；其他线程如果存在，只作为系统内部执行面或历史来源，由系统自行生成、吸收、关闭

## 管理职责落在哪里

不存在一个必须长期在线的“管理 agent 的 agent”。

当前真实设计是：

- `Supervisor / Product Anchor` 负责目标压缩、主线裁决和状态推进
- `Skeptic / Quality Anchor` 负责独立审查或显式降级
- 管理真相落在 `state + context + gate`，不是落在一个永久聊天线程上
