# DD Hermes 当前任务重校准与线程策略

## 当前单一判断

- 一期已经 `phase done`。
- 最近归档的 phase-2 proof task 已更新为 `dd-hermes-independent-skeptic-dispatch-v1`。
- 最近归档的治理主线仍是 `dd-hermes-successor-triage-v1`。
- 当前没有 active mainline。
- 当前线程继续作为唯一默认指挥线程。
- repo 当前不再卡在“独立 skeptic 是否只是命名元数据”，因为这条 proof 已经把 lane、execution closeout、quality review、integration truth 都收口了。
- 当前真实停点重新回到 successor 问题：下一条 bounded task 是否已经有足够 repo evidence 可以立新主线；如果没有，就诚实维持“暂无 active mainline”。

## 最近这条 proof 刚刚证明了什么

`dd-hermes-independent-skeptic-dispatch-v1` 刚刚完成并归档，核心结论是：

1. 独立 skeptic 已经不是 state 里的名字，而是有自己 handoff、context/runtime packet、worktree 和 gate truth 的真实 lane。
2. `dispatch-create` 在 `state_read`、`context_build`、`worktree_create` 上都能返回协议化 blocked JSON，所以监工和主线程能消费同一条 failure contract。
3. 这条 proof 已经有 review-backed execution closeout，execution anchor 是 `2db66973abd117bcaf752271d7f9f02e56fa03bd`，并已合并进 `main`，merge commit 是 `2ca44a84926c4242e4050342a9667a258ddda92a`。
4. archive 后 repo 又回到了诚实的“最近 proof 已归档、当前没有 active mainline”状态。

## 最近归档 proof 到底做了什么

`dd-hermes-independent-skeptic-dispatch-v1` 的目标只有一条：

- 让 DD Hermes 在需要独立质量位时，能把“独立 skeptic”从命名真相推进成运行面真相，并在 Codex 环境里留下可派发、可审查、可验收的证据链。

当前已完成的 bounded slice：

- `dispatch-create` 现在会为独立 skeptic 物化真实 review lane，而不是只给名义角色
- `context-build` / `state-read` / `thread-switch-gate` 能稳定暴露 `skeptic_lane ready|blocked|not-required`
- `dispatch-create` 对 preflight 和 lane materialization failure 都会回统一 blocked payload
- `tests/smoke.sh` 已证明 `state_read / context_build / worktree_create` 三类 dispatch failure surface
- 当前主线自己的 `expert-a` executor lane 与 `expert-b` skeptic lane 已经完成真实 execution slice、quality review、integration 和 archive 证据

不在这条已归档 proof 里的内容：

- 新的 runtime、gateway、provider 或 plugin-loader
- 再造一个新的多线程产品面
- 把 independent skeptic proof 扩成 generic orchestration cleanup

## 当前 phase-2 状态

- `planning`：已完成
- `candidate-pool cleanup`：已完成
- `successor selection`：上一轮已完成；accepted path 是 `dd-hermes-independent-skeptic-dispatch-v1`
- `skeptic lane materialization`：已完成
- `dispatch failure reporting hardening`：已完成
- `execution slice + quality review + closeout`：已完成
- `integration + archive`：已完成

当前真实停点已经收口成：

- 最近 proof 已归档：`dd-hermes-independent-skeptic-dispatch-v1`
- 最近治理裁决已归档：`dd-hermes-successor-triage-v1`
- 当前 active mainline：暂无
- repo 不再卡在 proof 补洞或 lane 物化；现在只剩 successor triage / 新 task package 是否成立

## 现在应该做什么

当前唯一合理的 phase-2 工作，不是重开已归档 proof，也不是再把这条 proof 伪装成“仍在执行”，而是：

1. 先看候选任务是否有足够 committed repo evidence
2. 如果有，就立新的 `contract + state + context + handoff + worktree`
3. 如果没有，就保持“暂无 active mainline”，不要伪造进度

当前不应该做的事情：

- 重新打开 `dd-hermes-explicit-gate-verdicts-v1`、`dd-hermes-quality-seat-escalation-rules-v1` 或 `dd-hermes-independent-skeptic-dispatch-v1`
- 为了看起来“更大”而扩成 runtime、provider 或 scheduler 任务
- 因为最近 proof 做完了，就跳过 successor triage 直接硬立下一条主线

## 当前主线裁决

当前结论已经更新为：**`dd-hermes-independent-skeptic-dispatch-v1` 已经 archive；当前没有 active mainline。**

理由：

1. 这条 proof 的产品边界已经闭合，继续把它挂成 active 只会制造假进度。
2. execution、quality review、closeout、integration、archive 证据都已经齐备。
3. 下一条 task 是否应该存在，已经重新变成一个独立裁决问题，而不是这条 proof 的“尾巴”。

## 继续编码前最小读取集合

1. `openspec/archive/dd-hermes-independent-skeptic-dispatch-v1.md`
2. `workspace/decisions/independent-skeptic-dispatch-routing/synthesis.md`
3. `workspace/closeouts/dd-hermes-independent-skeptic-dispatch-v1-expert-a.md`
4. `workspace/handoffs/dd-hermes-independent-skeptic-dispatch-v1-lead-to-lead-archive.md`
5. `指挥文档/06-一期PhaseDone审计.md`

## 现在需不需要第二线程

不是默认需要。

当前更合理的形态仍然是：

- `一个主线程 + 线程内多角色切换 + 按需隔离 worktree`

最近这条 proof 已经说明，真正需要的不是用户管理第二聊天线程，而是 DD Hermes 内部把 skeptic 角色物化成真实 review lane，并继续由主线程消费结果。

## 多 Agent 怎么用

- 架构、控制面、线程模型、权限/安全边界，默认先走 `3-explorer-then-execute`
- 这里的“多 agent”首先是当前线程内的协议角色与内部 explorer 收敛，不要求用户手动管理多个聊天线程
- 用户只保留当前线程作为对话面；其他线程如果存在，只作为系统内部执行面或历史来源，由系统自行生成、吸收、关闭

## 管理职责落在哪里

不存在一个必须长期在线的“管理 agent 的 agent”。

当前真实设计是：

- `Supervisor / Product Anchor` 负责目标压缩、主线裁决和状态推进
- `Skeptic / Quality Anchor` 负责独立审查或显式降级
- 管理真相落在 `state + context + gate`，不是落在一个永久聊天线程上
