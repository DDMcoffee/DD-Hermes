# DD Hermes 当前任务重校准与线程策略

## 当前单一判断

- 一期已经 `phase done`。
- 最近归档的 phase-2 proof task 是 `dd-hermes-quality-seat-escalation-rules-v1`。
- 当前 active mainline 已切到 `dd-hermes-explicit-gate-verdicts-v1`。
- 当前线程继续作为唯一默认指挥线程。
- 刚归档的主线已经完成两条 bounded slice：初始 T0-T4 矩阵，以及 `T2` 手动升级规则。
- 当前真实停点已经从“选择下一条窄主线”变成“把显式 gate verdict 主线推进到 closeout / integration / archive”。

## 刚归档的主线到底完成了什么

`dd-hermes-quality-seat-escalation-rules-v1` 的目标只有一条：

- 把 DD Hermes 的初始任务分类矩阵压成共享控制面的真实规则，让维护者能明确知道哪些任务类允许 `degraded`，哪些任务类必须 `requires-independent`。

当前这条主线已经完成的第一条 execution slice 包括：

- 冻结初始矩阵：`T0/T1/T2 => degraded-allowed`，`T3/T4 => requires-independent`
- 把 `task_class / quality_requirement / task_class_rationale` 落进 `state / context / dispatch / gates / schema / sprint bootstrap`
- 回填历史 v2 proof task，使旧任务在新规则下仍能通过 schema revalidation

当前这条主线继续完成的第二条窄 execution slice 包括：

- 明确 `T2` 不再低风险时的第一批手动升级条件：`high_risk_mode`、`integration_pressure`、`repeated_verification_failures`
- 让 `state / context / dispatch / thread-switch-gate / quality-gate / smoke` 同时暴露并执行 `T2 -> requires-independent` 的显式升级要求
- 保留一条真正的 `T2 degraded-allowed` 路径，同时证明一条 `T2` 在命中升级条件后会被阻塞，直到维护者显式提升 `quality_requirement`

不在本主线里的内容：

- 新的 Hermes runtime、gateway、provider、plugin loader
- 为了“像多 agent”而额外制造新的聊天线程
- 把治理任务和无关功能扩展混成一个大 task

## 当前 phase-2 状态

- `planning`：已完成
- `decision synthesis`：已完成
- `successor selection`：已完成，accepted path 为 `dd-hermes-explicit-gate-verdicts-v1`
- `explicit verdict persistence slice`：已落地并通过 workflow/context/schema/smoke 验证
- `execution_closeout persisted verdict slice`：进行中，本轮把 closeout semantic truth 纳入 `state.verdicts`
- `closeout / integration / archive`：未完成

当前真实停点不是“下一条主线选什么”，而是：

- 新主线已经建立
- 第一条 execution slice 已经落地并验证
- 下一步应继续收口这条主线，而不是重新回到 successor 裁决

## 当前唯一合理的下一步

当前唯一合理的后续动作，是继续推进 `dd-hermes-explicit-gate-verdicts-v1`：

1. 保持当前主线边界不扩散
   - 继续围绕显式 verdict 字段、summary surface、gate output、closeout truth 收口
2. 在这条主线内完成 closeout / integration / archive
   - 不要在第一条 slice 刚落地后又跳去重开 successor 讨论

## 当前 successor 裁决

当前结论已经更新为：**选定 `dd-hermes-explicit-gate-verdicts-v1` 作为新的 active mainline。**

理由：

1. `explicit gate verdict fields`
   - 这是 `dd-hermes-anchor-governance-v1` 明确留下的开放问题，而且它正好补上当前维护者最直接的控制面缺口：state 里缺少稳定的 gate verdict truth。
2. `explicit task metadata for routing`
   - 这是有效候选，但当前仓库还没有比 verdict persistence 更强的 maintainer-visible breakage 证据。
3. `dd-hermes-s5-2expert-20260416`
   - 它仍然是实验，不是 ready successor。

所以这次不再维持 `current_mainline_task_id` 为空；仓库证据已经足够明确地偏向 verdict persistence 这条窄边界。

当前不应该做的事情：

- 重新打开 `dd-hermes-quality-seat-escalation-rules-v1`
- 再把 `T2` override 规则扩成无边界的风险百科
- 把主线扩回线程模型、runtime 或 provider 层

## 继续编码前最小读取集合

1. `openspec/archive/dd-hermes-quality-seat-escalation-rules-v1.md`
2. `workspace/state/dd-hermes-quality-seat-escalation-rules-v1/state.json`
3. `workspace/handoffs/dd-hermes-quality-seat-escalation-rules-v1-lead-to-lead-archive.md`
4. `workspace/decisions/quality-seat-escalation-routing/synthesis.md`
5. `workspace/contracts/dd-hermes-explicit-gate-verdicts-v1.md`
6. `workspace/decisions/explicit-gate-verdicts-routing/synthesis.md`
7. `指挥文档/06-一期PhaseDone审计.md`

## 现在需不需要第二线程

不是默认需要。

当前更合理的形态仍然是：

- `一个主线程 + 线程内多角色切换 + 按需隔离 worktree`

只有同时满足下面条件，才值得外派新的 execution slice：

- 已有明确 `task_id`
- 已有 `contract + state + context + handoff + worktree`
- 已有 `synthesis_path`
- 写集边界稳定
- 额外 worktree 能真实降低冲突或回归风险

如果当前工作还是 successor 裁决、证据回填或文档收口，就不该开第二线程。

## 多 Agent 怎么用

- 架构、控制面、线程模型、权限/安全边界，默认先走 `3-explorer-then-execute`
- 这里的“多 agent”首先是当前线程内的协议角色与内部 explorer 收敛，不要求用户手动管理多个聊天线程
- 用户只保留当前线程作为对话面；其他线程如果存在，只作为系统内部执行面或历史来源，由系统自行生成、吸收、关闭

## 管理职责落在哪里

不存在一个必须长期在线的“管理 agent 的 agent”。

当前真实设计是：

- `Supervisor / Product Anchor` 负责目标压缩、主线裁决和状态推进
- `Skeptic / Quality Anchor` 负责独立审查或显式降级
- 管理真相落在 `state + context + gate`，不是落在一个永久聊天线程上
