# DD Hermes 当前任务重校准与线程策略

## 当前单一判断

- 一期已经 `phase done`
- 当前 phase-2 唯一主线 `dd-hermes-anchor-governance-v1` 已归档
- 当前线程继续作为唯一默认指挥线程
- phase-2 主线现在已经有正式 `contract + state + proposal + handoff + decision synthesis + context + worktree + archive`

所以，这一阶段的主目标不是“再补一个外部执行线程”，而是把恒定锚点治理正式落到仓库协议里。

## 当前主线到底做什么

`dd-hermes-anchor-governance-v1` 的目标只有一条：

- 把 `Product Anchor` 和 `Quality Anchor` 从文档概念推进到 `state / dispatch / context / gate / schema` 的硬约束。

不在本主线里的内容：

- 新的 Hermes runtime、gateway、provider、plugin loader
- 为了“像多 agent”而额外制造新的聊天线程
- 把治理任务和无关功能扩展混成一个大 task

## 当前任务拆分

### Task A：锚点治理真相审计

- 核对 contract、state、dispatch、gate、schema 里哪些字段已经有，哪些还只是口头要求
- 产出单一缺口清单，不边审边扩写无关功能

状态：

- 已完成

### Task B：锚点协议落盘

- 把 `product_goal / user_value / non_goals / drift_risk`
- 以及 `quality review / independent_skeptic / degraded truth`
- 变成控制面真实字段和 gate 条件

状态：

- 已完成并归档

### Task C：验证与入口回写

- 用 smoke / schema / demo entry 证明锚点治理已经生效
- 把 phase-2 主线状态回写到 `README.md`、`06`、`08` 和 task 工件

状态：

- 已完成并归档

## 现在需不需要第二线程

不是默认需要。

当前更合理的形态仍然是：

- `一个主线程 + 线程内多角色切换 + 按需隔离 worktree`

只有同时满足下面条件，才值得开一个 execution slice：

- 已有明确 `task_id`
- 已有 `contract + state + context + handoff + worktree`
- 已有 `synthesis_path`
- 写集边界稳定
- 额外 worktree 能真实降低冲突或回归风险

如果当前工作是治理、裁决、归档、审计、文档收口或 trace 回填，就不该开第二线程。

## 多 Agent 怎么用

- 架构、控制面、线程模型、权限/安全边界，默认先走 `3-explorer-then-execute`
- 这里的“多 agent”首先是当前线程内的协议角色与内部 explorer 收敛，不要求用户手动管理多个聊天线程
- 用户只保留当前线程作为对话面；其他线程如果存在，只作为系统内部执行面或历史来源，由系统自行生成、吸收、关闭

## 管理职责现在落在哪里

不存在一个必须长期在线的“管理 agent 的 agent”。

当前真实设计是：

- `Supervisor / Product Anchor` 负责目标压缩、主线裁决和状态推进
- `Skeptic / Quality Anchor` 负责独立审查或显式降级
- 管理真相落在 `state + context + gate`，不是落在一个永久聊天线程上

## 当前裁决

- 文档收口、主线校准、锚点策略定义，由当前线程继续推进
- phase-2 已经进入真实 task-bound 实现
- 当前仍不把第二线程当默认主线；只有出现明确、独立的 execution slice 时才外派
