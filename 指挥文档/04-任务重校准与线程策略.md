# DD Hermes 当前任务重校准与线程策略

## 当前单一判断

- 一期已经 `phase done`。
- 最近归档的 phase-2 proof task 已更新为 `dd-hermes-smoke-all-progress-v1`。
- 最近归档的治理主线已更新为 `dd-hermes-residue-remediation-hints-v1`。
- 当前没有 active mainline。
- 当前线程继续作为唯一默认指挥线程。
- repo 当前不再卡在“successor evidence 只能靠手工 sweep 判断”，而且 residue 也不再只是被动忽略，因为控制面现在已经能给出 remediation hints。
- repo 当前也不再卡在“`bash tests/smoke.sh all` 跑着但完全无感知”；authoritative full gate 已能持续暴露 section progress truth。
- 当前真实停点已经回到“没有 active mainline；最近 proof 已归档；下一步只能等待新的 committed task package”。

## 最近这条 proof 刚刚证明了什么

`dd-hermes-smoke-all-progress-v1` 刚刚完成并归档，核心结论是：

1. `bash tests/smoke.sh all` 现在会在 stderr 上持续输出顶层 `start / done / fail` section markers，不再是沉默长跑。
2. 这条 proof 保留了原来的 stdout success contract；full gate 仍以最终 JSON 行作为成功信号，不需要改现有消费面。
3. 这条 proof 已经有 review-backed execution closeout，execution anchor 是 `309e1472189d419a56e98e87271b4d2b582bc13e`，并已整合进 shared root，integration commit 是 `a066124db6667ff4b83f0d7cb00dd972700dfc4d`。
4. archive 后 repo 又回到了诚实的“最近 proof 已归档、当前没有 active mainline”状态。

## 最近归档 proof 到底做了什么

`dd-hermes-smoke-all-progress-v1` 的目标只有一条：

- 让 DD Hermes 在执行 authoritative full smoke gate 时，能直接告诉维护者“现在还在跑、当前跑到哪、如果停了是停在哪一段”，而不是只剩一个静默进程。

当前已完成的 bounded slice：

- `tests/smoke.sh all` 现在会包裹顶层 section 执行并吐出 stderr 进度真相
- `tests/smoke.sh progress` 新增了一个窄测试面，用来验证 progress contract
- `tests/smoke.sh` 的 `entry` 断言也已经做了去脆弱化处理，不再把一个 synthetic residue id 是否出现在截断摘要里当成硬条件
- 当前主线自己的 `expert-a` execution slice 已完成真实 execution commit、shared-root integration 和 archive 证据

不在这条已归档 proof 里的内容：

- 新的 runtime、gateway、provider 或 plugin-loader
- 再造一个新的 smoke runner 产品
- 把 stderr progress 扩成新的自动化 streaming protocol

## 当前 phase-2 状态

- `planning`：已完成
- `candidate-pool cleanup`：已完成
- `successor selection`：上一轮已完成；accepted path 是 `dd-hermes-independent-skeptic-dispatch-v1`
- `skeptic lane materialization`：已完成
- `dispatch failure reporting hardening`：已完成
- `execution slice + quality review + closeout`：已完成
- `integration + archive`：已完成
- `successor evidence audit`：已完成并 archive（`dd-hermes-successor-evidence-audit-v1`）
- `smoke progress truth`：已完成并 archive（`dd-hermes-smoke-all-progress-v1`）

当前真实停点已经收口成：

- 最近 proof 已归档：`dd-hermes-smoke-all-progress-v1`
- 最近治理裁决已归档：`dd-hermes-residue-remediation-hints-v1`
- 当前 active mainline：暂无
- repo 不再卡在 successor evidence audit，也不再卡在 smoke full gate 的黑箱执行；最近一轮 residue remediation slice 也已经归档，并把 residue 处理建议压进了控制面，当前无 active mainline

## 现在应该做什么

当前唯一合理的下一步，不是重开已归档 proof，也不是从 residue 或聊天记忆里硬凿新主线，而是：

1. 只在 repo evidence 明确支持新的 bounded task package 时，再重新写入 `current_mainline_task_id`
2. 新主线必须绑定真实的 contract / state / handoff / verification，而不是从聊天记忆或本地残留倒推
3. 在那之前，维持“当前没有 active mainline”的诚实状态

当前不应该做的事情：

- 重新打开 `dd-hermes-successor-evidence-audit-v1`、`dd-hermes-independent-skeptic-dispatch-v1` 或其他已归档 proof
- 为了看起来“更大”而扩成 runtime、provider 或 scheduler 任务
- 因为最近 proof 做完了，就从 residue 或聊天记忆里硬立下一条 feature 主线

## 当前主线裁决

当前结论已经更新为：**`dd-hermes-smoke-all-progress-v1`、`dd-hermes-successor-evidence-audit-v1`、`dd-hermes-successor-triage-v2` 与 `dd-hermes-residue-remediation-hints-v1` 都已 archive；当前没有 active mainline。**

理由：

1. 最近一轮 proof 已经把 smoke full gate 的可观察性补成 shared-root truth；这条 bounded gap 已经完成。
2. 更早的 successor evidence 与 residue remediation 两条 bounded gap 也已经完成并归档。
3. 在没有新的 bounded task package 前，repo 仍然只能诚实停在“no active mainline”。

## 继续编码前最小读取集合

1. `openspec/archive/dd-hermes-smoke-all-progress-v1.md`
2. `openspec/archive/dd-hermes-successor-evidence-audit-v1.md`
3. `openspec/archive/dd-hermes-residue-remediation-hints-v1.md`
4. `workspace/contracts/dd-hermes-smoke-all-progress-v1.md`
5. `指挥文档/06-一期PhaseDone审计.md`

## 现在需不需要第二线程

不是默认需要。

当前更合理的形态仍然是：

- `一个主线程 + 线程内多角色切换 + 按需隔离 worktree`

最近这条 proof 已经说明，真正需要的不是用户管理第二聊天线程，而是 DD Hermes 内部把 skeptic 角色物化成真实 review lane，并继续由主线程消费结果。

## 多 Agent 怎么用

- 架构、控制面、线程模型、权限/安全边界，默认先走 `3-explorer-then-execute`
- 这里的“多 agent”首先是当前线程内的协议角色与内部 explorer 收敛，不要求用户手动管理多个聊天线程
- 用户只保留当前线程作为对话面；其他线程如果存在，只作为系统内部执行面或历史来源，由系统自行生成、吸收、关闭

## 管理职责落在哪里

不存在一个必须长期在线的“管理 agent 的 agent”。

当前真实设计是：

- `Supervisor / Product Anchor` 负责目标压缩、主线裁决和状态推进
- `Skeptic / Quality Anchor` 负责独立审查或显式降级
- 管理真相落在 `state + context + gate`，不是落在一个永久聊天线程上
