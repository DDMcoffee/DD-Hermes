---
task_id: dd-hermes-demo-entry-v1
owner: lead
from: lead
to: execution-thread
scope: user-visible experience entry for DD Hermes phase-1
state_path: 待指挥线程创建
handoff_paths:
  - 待指挥线程创建
contract_path: 待指挥线程创建
---

# 执行线程干到底说明

## 目标

- 不再继续补新的零散控制面能力，而是把已经证明过的一次体验版闭环变成用户可见入口。
- 让一个没看过大量内部文档的人，能通过一个命令和一页中文说明看懂 DD Hermes 目前已经做到哪里。

## 终点

- 形成一个只读的用户可见入口，至少包含：
- 一个脚本命令，能输出当前体验版状态、最近一次真实证明和下一条主线任务。
- 一页中文 Markdown 落地页，解释这个入口在看什么、为什么它代表当前体验版本。
- 执行线程在隔离 worktree 中完成实现，形成 execution commit，并回写 handoff / verification。
- Lead 能通过现有 `git-integrate-task`、schema 校验和 archive 流程把它收成 `done/archive`。

## 禁止事项

- 不要修改项目级 policy。
- 不要直接在主工作区提交 execution slice。
- 不要把未验证的猜测写进 handoff 或 state。
- 不要把入口脚本做成控制器或调度器。
- 一旦发现 finish line 已经从“代码实现”变成“项目目标裁决”，立即停回指挥线程。

## 执行顺序

1. 先读取当前 task 的 `contract + state + handoff + context`，确认这是 `dd-hermes-demo-entry-v1`。
2. 在 worktree 内实现一个只读入口脚本和一个中文入口页面。
3. 用 repo 事实填充“当前体验版状态 / 最近一次证明 / 下一条主线任务”，不要手写漂移性结论。
4. 完成验证、handoff 和 execution commit，再交回指挥线程做 integration / archive。

## 最小实现范围

- 一个只读入口脚本
- 一页中文入口说明
- 为入口增加的最小 README / 指挥文档索引更新
- 至少一条脚本级验证链路和一条任务级 schema 校验链路

## 交付物

- 体验入口脚本
- 中文入口说明
- execution commit
- verification 证据
- expert handoff

## 回写要求

- 回写 `workspace/handoffs/*expert-to-lead.md`
- 回写当前 task state 的 verification / git 锚点
- 回写必要的 note，说明是否已经达到 `execution slice done`

## 何时停

- 当 `dd-hermes-demo-entry-v1` 已达到 `execution slice done`，且入口脚本、入口说明、execution commit、handoff、verification、state 锚点都完成时停止。
- 遇到新的架构歧义、项目级目标冲突、越权修改需求或硬性验证阻塞时停止，并回到指挥线程。
