---
task_id: dd-hermes-endpoint-schema-v1
owner: lead
from: lead
to: execution-thread
scope: unified coordination endpoint schema and execution closeout support
state_path: 待指挥线程创建
handoff_paths:
  - 待指挥线程创建
contract_path: 待指挥线程创建
---

# 执行线程干到底说明

## 目标

- 把三层终点定义继续推进成真正可执行的 endpoint/schema 实现，而不是只停在指挥文档里。
- 让 DD Hermes 的下一轮执行线程有明确的 schema、closeout 模板和测试护栏。

## 终点

- `docs/coordination-endpoints.md` 或同等级规范文档落地，明确三层终点和字段 schema。
- 增加至少一份执行收尾模板，例如 `.codex/templates/EXECUTION-CLOSEOUT.md`。
- 增加能验证 contract / handoff / state / closeout 必填字段的测试或检查脚本。
- 形成 execution commit，并把验证证据回写给指挥线程。

## 禁止事项

- 不要修改项目级 policy。
- 不要直接在主工作区提交 execution slice。
- 不要把未验证的猜测写进 handoff 或 state。
- 一旦发现 finish line 已经从“代码实现”变成“项目目标裁决”，立即停回指挥线程。

## 执行顺序

1. 先把三层终点文档拆成可执行 schema 和 closeout 模板。
2. 再补脚本或测试，让这些 schema 至少能被 smoke 或专门检查脚本验证。
3. 最后在 worktree 内完成验证、写回 handoff 和 execution commit。

## 最小实现范围

- `docs/coordination-endpoints.md`
- `docs/artifact-schemas.md`
- `.codex/templates/EXECUTION-CLOSEOUT.md`
- 至少一条测试或 schema 校验链路

## 交付物

- endpoint/schema 文档
- closeout 模板
- 测试或检查脚本改动
- execution commit
- verification 证据
- expert handoff

## 回写要求

- 回写 `workspace/handoffs/*expert-to-lead.md`
- 回写当前 task state 的 verification / git 锚点
- 回写必要的 note，说明是否已经达到 `execution slice done`

## 何时停

- endpoint/schema、模板、验证三者都落地，且 execution commit、handoff、verification、state 锚点都完成时停止。
- 遇到新的架构歧义、项目级目标冲突、越权修改需求或硬性验证阻塞时停止，并回到指挥线程。
