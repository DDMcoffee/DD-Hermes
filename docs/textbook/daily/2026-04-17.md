# Daily Textbook Summary

## Date

- 2026-04-17

## Progress Note

- 今天的 DD Hermes 像是在补两道门。第一道门先问“这件事到底是不是主线任务”，第二道门再问“代码改完以后有没有认真复查”。这样执行线程不能只顾着往前跑，还要先讲清目标，再拿出验证和复核证据。
- 用户今天也把使用方式说得更明白：真实对话尽量留在一个主线程里，复杂架构题先走 `3-explorer-then-execute`，内部线程可以有，但不能把线程管理责任丢给用户。

## New Material

- 新增 `指挥文档/09-今日输入整理.md`，把当天输入过的目标、偏好、线程要求和教材 agent 需求集中收口。
- 新增 `memory/user/user-pref-single-command-thread.md`，确认“用户只在当前主线程对话，其他线程由系统内部管理”。
- 完成 `workspace/contracts/dd-hermes-anchor-governance-v1.md`、`workspace/handoffs/dd-hermes-anchor-governance-v1-lead-to-expert-a.md`、`openspec/proposals/dd-hermes-anchor-governance-v1.md` 和 `workspace/decisions/anchor-governance-routing/synthesis.md`，把 phase-2 从“路线想法”落成“真实任务包”。
- `70db9d3` 这次提交把 `dispatch-create`、`thread-switch-gate`、`quality-gate`、state/context/schema/smoke 一起加固，目标是让 `Product Anchor` 和 `Quality Anchor` 真正卡住错误流程。
- 今天还有一批未提交的 closeout/schema 语义校验补强，说明主线已经从“有没有字段”往“这些字段能不能真的宣称完成”推进。

## Confirmed Patterns

- 复杂架构题先讨论再执行；先把 `contract + state + context + handoff` 立起来，执行线程才不容易跑偏。
- 能用现有 `state + context + gate` 讲清楚的约束，就不要再发明新的常驻 manager runtime。
- 运行时展示的目标必须和 `product.goal` 对齐，不能只剩容易漂移的 `lease.goal` 口号。
- 质量复核不是最后补一句“看过了”，而是 completion gate 的一部分。
- 用户偏好也要写进 memory，不然线程管理很快又会退回“让用户自己盯线程”的旧路。

## Key Input Links

- `codex://threads/019d8a45-0205-7ca3-aff8-2a39d3d9dc15`
- `AGENTS.md`
- `README.md`
- `指挥文档/08-恒定锚点策略.md`
- `指挥文档/09-今日输入整理.md`
- `workspace/contracts/dd-hermes-anchor-governance-v1.md`
- `workspace/decisions/anchor-governance-routing/synthesis.md`
- `workspace/state/dd-hermes-anchor-governance-v1/state.json`

## Open Gaps

- `workspace/closeouts/dd-hermes-anchor-governance-v1-expert-a.md` 还是模板占位，`execution_commit` 为空，`quality_review_status` 仍是 `pending`，还不能老实地说 “execution slice done”。
- `state.team.role_integrity.independent_skeptic=false` 依旧成立，说明质量锚点暂时还是降级形态。
- `docs/textbook/entries/` 和 `docs/textbook/sources/` 还没有把今天的经验拆成独立教材条目，所以今天的日报更像“总复习”，还不是“分课讲义”。
- 当前工作区还有一批未提交的语义校验改动，是否全部通过 `tests/smoke.sh all`，今天这份日报不能替它背书。

## Next Chapter

- 下一章建议写《有字段不等于能毕业：怎样让 execution closeout 真正可验收》。
- 这一章要把 `workspace/closeouts/*.md`、`quality-gate`、`check-artifact-schemas.sh` 和 smoke 证据连成一条线，教清楚“模板、证据、通过”三者的区别。
