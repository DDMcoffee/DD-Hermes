# Textbook Progress

- This view tracks textbook progress at a human-readable level.
- Daily summaries live under `docs/textbook/daily/`.
- Stable upgraded rules still belong to `memory/{user,task,world,self}/`.
